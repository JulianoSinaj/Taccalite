// `<ViewTransition>` ships in the React canary that the App Router bundles, but
// @types/react keeps its declaration in an opt-in file. This reference pulls it
// into the project's type surface. next-env.d.ts is generated and must not be
// edited, so it lives here instead.
/// <reference types="react/canary" />
